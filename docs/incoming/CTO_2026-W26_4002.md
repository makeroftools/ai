# ContextVolley: AI:@CTO → @GTM

> Persisted from CV: CTO_2026-W26_4002
> Date: 2026-06-25 (W26 D4)
> Context: @CTO (Nik Cimino) requesting 5 definitions from @GTM to unblock W26 execution

---

## Original Reference

- **File:** `weown-workspace/scratch/` (transcribed from user chat)
- **CCC-ID:** `CTO_2026-W26_4002`
- **From:** AI:@CTO @ INT-P08 (Dev.WeOwn.Tools)
- **To:** @GTM (Jason)
- **CC:** @MOT + #MetaCouncil

---

## The 5 Asks

### 1. MAIT + OpenTofu Assignment

> *"I need the scope pinned down: which MAIT am I building, on which instance, and how does OpenTofu fit? Am I making OpenTofu run as a no-code tools-agent (an OpenTofu MAIT), or building the MAIT that wraps OpenTofu (a meta-layer that calls OpenTofu as one of its tools)? What does 'done' look like, and by what day of W26?"*

### 2. Confirm "Best MAIT Builder This Week" Lens

> *"Tell me which MAIT you want @CTO owning first. And name the FedArc grounding every new MAIT must reference — SharedKernel plus which _SYS_/GUIDE docs — so all MAITs are built to one standard, not twelve different styles."*

### 3. Charters for PRJ-405, 406, 444

> *"I need two-to-three line charters for each of:*
> - *PRJ-405 — WeOwn Dev On-Chain Cooperative*
> - *PRJ-406 — Git Architecture*
> - *PRJ-444 — MAITs Matrix (your stated #1 priority)*
>
> *Also — does PRJ-405 carry the 'get the devs operating in OpenClaw' workstream, or is that separate?"*

### 4. Interim Git Rule Until PRJ-406 Lands

> *"Kill the PR-review process now — commit straight to main, capture via CCC-ID — or hold the current flow one more week? Changes are sitting in PRs waiting on this decision, and the team is blocked."*

### 5. Meta-Agent Claude Rebuild

> *"Where should the fresh home instance live? Is DeepNote.com the sanctioned tool for extracting the collective intelligence? Who owns the rebuild?"*

---

## Relevance to @MOT 🛠️

| Signal | Implication for Us |
|--------|-------------------|
| **PRJ-444 = #1 priority** for @GTM | Our MAIT convention is directly on-target. This is the priority. |
| @CTO asking for FedArc grounding docs | Our `MAITS-CONVENTION.md` already defines the doc references (SharedKernel, D-031/D-032, PRJ-469 §9). This validates our approach. |
| @CTO needs *which* MAIT to build first | We chose Keycloak, AnythingLLM, Paperless. If @GTM gives @CTO a different first MAIT, we align. |
| PRJ-405 (WeOwn Dev coop) + PRJ-406 (Git) on parallel track | These are @CTO's lane — doesn't conflict with our MAIT content work. |
| Interim Git rule pending | If @GTM says "commit straight to main + CCC-ID," our branch strategy may simplify. Currently branch + PR is correct per root AGENTS.md. |
| Meta-Agent Claude rebuild pending | Tooling for collective intelligence extraction — relevant if it becomes the META layer our MAITs report to. |